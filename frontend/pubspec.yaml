name: calbot
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  syncfusion_flutter_calendar: ^31.2.12

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
